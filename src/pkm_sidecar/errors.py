"""Typed exception hierarchy (single source of truth).

Reserved for the logging_errors subsystem, which replaces this stub with the
canonical ``PkmSidecarError`` hierarchy imported by every other module
(PRD §17). Empty in the scaffold.
"""
