"""Read-only (GET-only) async Joplin Data API client.

Reserved for the joplin_client subsystem (PRD §10). v0.1 hard invariant: this
module must never gain write/delete methods. Empty in the scaffold.
"""
